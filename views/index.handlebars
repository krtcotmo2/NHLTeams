<div class="cotainer col-8 mx-auto">
     <h1>Index Page</h1>
     <div class="row">
          <div class="col-6">
               <h2>Eastern Confrence</h2>
               {{#each eastTeams}}
                    <p>{{cityName}} {{teamName}}</p>
               {{/each}}
          </div>
          <div class="col-6">
               <h2>Western Confrence</h2>
               {{#each westTeams}}
                    <p>{{cityName}} {{teamName}}</p>
               {{/each}}
          </div>
     </div>
</div>